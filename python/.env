# .env for Google Drive backup integration
# Place this file in the python/ directory
#
# NOTE: This .env is for Python scripts only. Do not place it in the global env/ folder.
#       Global/service environment files (for containers and shell scripts) belong in env/.

# Path to your Google API credentials file
GDRIVE_CREDENTIALS=credentials.json

# Path to the token file (auto-generated after first auth)
GDRIVE_TOKEN=token.pickle

# Default Google Drive folder for backups
GDRIVE_DEFAULT_FOLDER=FoundryVTT-Backups
